var fs  = require("fs"),
burrito = require("burrito");


/**
 * loads the file
 */

function load(source) {
	return fs.readFileSync(source);
}

function parse(source) {
	var src = burrito(source, function(node) {
		if(node.name == "call" && node.start.value == "clump") {
			node.wrap("test");
		}
	});

	console.log(src)
}

function clump() {

}


var test = {
	name: clump(__filename)
};

parse(fs.readFileSync(__filename));